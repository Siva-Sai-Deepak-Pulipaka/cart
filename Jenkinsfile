@Library('roboshop') _

env.app_lang = "nodejs"
env.component = "cart"

pipeline {
    agent any

    stages {

        stage('Compile/Build'){
            steps {
                echo 'Compile/Build'
            }
        }
    stage('Test Cases'){
            steps {
                echo 'Test Cases'
            }
        }
    }   
}